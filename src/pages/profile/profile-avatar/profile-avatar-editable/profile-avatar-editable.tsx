import { Avatar } from "../../../../components";
import { AvatarProps } from "../../../../components/types";

const ProfileAvatarEditable = (props: AvatarProps) => (
  <Avatar {...props} size="larger" />
);

export default ProfileAvatarEditable;
